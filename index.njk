---
layout: base-layout.njk
page_title: NEWEST ESSAY
pagination:
  data: collections.essay
  size: 10
  reverse: true
  alias: essays
---

{% set essay = essays[0]%}

<time datetime="{{ essay.date | dateIso }}">{{ essay.date | dateReadable }}</time>
<h3 class="font-quarto essay-title">{{ essay.data.title }}</h3>
<section class="font-chron">{{ essay.templateContent | safe }}</section>

